[package]
name = "database-codegen"
version = "0.1.0"
edition = "2024"

[lib]
name = "database_codegen"
path = "src/lib.rs"

[dependencies]
nx_core = { path = "../nx-core" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
